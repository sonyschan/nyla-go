{
  "scripts": {
    "build:embeddings": "node pwa/js/rag/build-embeddings.js",
    "setup:hooks": "./scripts/setup-git-hooks.sh",
    "version:check": "node -e \"const vm = new (require('./pwa/js/rag/nyla-kb-version-manager.js'))(); vm.initialize().then(() => vm.getVersionInfo()).then(console.log).catch(console.error)\"",
    "version:clear": "node -e \"const vm = new (require('./pwa/js/rag/nyla-kb-version-manager.js'))(); vm.initialize().then(() => vm.clearVersion()).then(() => console.log('Version tracking cleared')).catch(console.error)\"",
    "embeddings:force": "NYLA_FORCE_REBUILD=true npm run build:embeddings"
  },
  "description": "Add these scripts to your package.json for KB version management"
}